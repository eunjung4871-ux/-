"use client"; import React, { useState } from "react"; import { useRouter } from "next/navigation"; import Link from "next/link"; import { Lock, ArrowLeft } from "lucide-react"; import { createClient } from "@/lib/supabase/client"; const h = React.createElement; export default function AdminLoginPage() { const router = useRouter(); const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [error, setError] = useState<string | null>(null); const [loading, setLoading] = useState(false); const handleLogin = async () => { setError(null); setLoading(true); const supabase = createClient(); const { error } = await supabase.auth.signInWithPassword({ email, password }); setLoading(false); if (error) { setError("이메일 또는 비밀번호가 올바르지 않아요."); return; } router.push("/admin"); router.refresh(); }; return h("div", { className: "relative flex min-h-screen flex-col items-center justify-center gap-4 px-6" }, h(Link, { href: "/", className: "absolute left-5 top-6 flex items-center gap-1 font-body text-sm text-violet-400" }, h(ArrowLeft, { className: "h-4 w-4" }), " 조회 화면으로"), h(Lock, { className: "h-8 w-8 text-violet-600" }), h("p", { className: "font-display text-lg text-violet-800" }, "관리자 로그인"), h("div", { className: "w-full max-w-xs space-y-2" }, h("input", { type: "email", value: email, onChange: (e: any) => setEmail(e.target.value), placeholder: "이메일", className: "w-full rounded-2xl border-2 border-violet-200 px-4 py-3 font-body outline-none focus:border-pink-400" }), h("input", { type: "password", value: password, onChange: (e: any) => setPassword(e.target.value), onKeyDown: (e: any) => e.key === "Enter" && handleLogin(), placeholder: "비밀번호", className: "w-full rounded-2xl border-2 border-violet-200 px-4 py-3 font-body outline-none focus:border-pink-400" }), error ? h("p", { className: "font-body text-xs text-rose-500" }, error) : null, h("button", { onClick: handleLogin, disabled: loading, className: "w-full rounded-2xl bg-violet-500 py-3 font-display text-white shadow-md shadow-violet-200 transition active:scale-95 disabled:opacity-60" }, loading ? "로그인 중..." : "로그인")), h("p", { className: "max-w-xs text-center font-body text-xs text-violet-400" }, "관리자 계정은 Supabase 대시보드의 Authentication 메뉴에서 미리 만들어두세요.")); }
