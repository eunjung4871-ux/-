"use client"; import React from "react"; import { useRouter } from "next/navigation"; import { LogOut } from "lucide-react"; import { createClient } from "@/lib/supabase/client"; const h = React.createElement; export function LogoutButton() { const router = useRouter(); const handleLogout = async () => { const supabase = createClient(); await supabase.auth.signOut(); router.push("/admin/login"); router.refresh(); }; return h("button", { onClick: handleLogout, className: "flex items-center gap-1 font-body text-sm text-violet-400 hover:text-rose-500" }, h(LogOut, { className: "h-4 w-4" }), " 로그아웃"); }
